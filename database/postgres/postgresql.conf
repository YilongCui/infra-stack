# PostgreSQL configuration for local development

# Network settings
listen_addresses = '*'
port = 5432

# Connection settings
max_connections = 100

# Memory settings (适合本地开发)
shared_buffers = 128MB

# Logging (可选，用于调试)
log_statement = 'none'
log_destination = 'stderr'
logging_collector = off
